# AI 外呼平台操作手册

> 适用版本：`26f205b`（本次应用代码基准）
> 手册更新时间：2026-09-08
> 适用对象：系统管理员、运营人员、质检人员、班组长和人工座席

[全部87张截图目录](screenshot-index.md)

## 1. 使用范围与重要说明

本手册说明 AI 外呼平台管理中心和座席中心的日常操作。截图来自本次以最新代码重新构建的独立 Docker 演示环境（18080 端口），页面中的号码、账号、任务和统计均为合成演示数据。正常安装默认端口为 8000。安装见 [INSTALL.md](../INSTALL.md)，详细参数见[平台配置手册](platform-configuration-guide.md)。截图采集、操作验证及未验证项见[本次验收记录](reviews/20260908-manual-refresh.md)。

- 管理中心负责客户、话术、任务、线路、容量、质检、报表、账号和系统配置。
- 座席中心负责单路呼叫、座席状态、人工接管和本人可见的通话记录。
- `Mock` 线路只用于开发验收，不能视为真实电话接通。
- 生产环境必须关闭演示账号，使用正式账号和密钥管理服务。
- 发起外呼、启动任务、重拨、转人工、挂断、批量禁打、删除和修改系统配置都会改变业务数据，操作前应确认对象和环境。

## 2. 访问地址与角色

| 入口 | 默认地址 | 可用角色 |
|---|---|---|
| 管理中心 | `http://127.0.0.1:8000/admin` | `admin` |
| 座席中心 | `http://127.0.0.1:8000/agent` | `agent` |
| API 文档 | `http://127.0.0.1:8000/docs.html` | 已登录用户可从右上角账号菜单进入 |
| 健康检查 | `http://127.0.0.1:8000/readyz` | 运维检查 |

本地演示账号仅在非生产环境且 `DEMO_USERS_ENABLED=true` 时创建：

- 管理员：`admin / 12345678`
- 座席：`1001@test / 12345678`

基础 Compose 仅绑定回环；远程访问需 SSH 隧道或另行配置 HTTPS 入口。生产环境不得继续使用上述密码。管理员和座席登录入口相互独立，座席直接访问管理页面会被重定向回座席中心。

## 3. 登录、语言和退出

1. 打开对应角色的登录地址。
2. 输入用户名和密码，点击“登录”。
3. 登录成功后，左侧显示该角色可访问的菜单，右上角显示服务状态、语言和当前账号。
4. 点击“中文 / English”可即时切换语言，语言偏好保存在当前浏览器中。
5. 退出时点击右上角“当前账号”→“退出登录”；退出后应返回对应登录页。

如果登录失败：

- 确认进入的是正确角色入口。
- 检查账号是否启用、密码是否正确。
- 连续失败并提示请求过多时，等待登录限流窗口结束后再试。
- 生产环境不要让用户共享管理员账号。

![登录表单](assets/manual-20260908/admin-login.png)

![退出登录入口](assets/manual-20260908/logout-menu.png)

## 4. 管理中心概览

管理员登录后首先进入仪表盘。仪表盘展示周期通话、有效接通、意向线索、平均质检分、最近通话、快捷操作和任务转化表现。

![管理员仪表盘](assets/manual-20260908/dashboard.png)

左侧菜单包括：仪表盘、质检复核、客户管理、话术管理、外呼任务、通话记录、客户运营、运营报表、分组监控、账单与计费、用户与座席、外呼线路、知识库、系统配置、业务交付策略、监控与审计。

推荐的首次配置顺序：

1. 用户与座席
2. 外呼线路
3. 系统配置
4. 客户管理或客户运营导入
5. 话术管理和知识库
6. 外呼任务
7. 通话记录和质检复核
8. 运营报表、账单与监控

## 5. 用户与座席

入口：管理中心 → 用户与座席。

### 新增账号

1. 点击“新增用户”。
2. 填写用户名、姓名、密码和可选手机号。
3. 选择角色：管理员或座席。
4. 如该座席需要质检复核权限，开启“班组长”。
5. 保持“启用”并保存。

密码至少 8 位。生产环境应使用随机强密码，并通过安全渠道交付。

### 编辑、停用和改密

- 点击“编辑”可修改姓名、手机号、角色和班组长状态。
- 编辑时填写新密码会同时重置密码；留空则不改密码。
- 列表中的“启用”开关可停用账号，但不能停用当前登录账号。
- 账号和权限修改会进入审计日志。

![新增用户表单](assets/manual-20260908/users-form.png)

保存后刷新列表，再用新账号登录验证权限。

## 6. 客户管理与客户运营

### 单个客户

入口：管理中心 → 客户管理。

![客户管理](assets/manual-20260908/contacts.png)

点击“新增客户”，填写：

- 手机号：新建后不可直接修改；同一租户内不可重复。
- 姓名和标签：用于检索、分组与运营分析。
- 同意状态：已同意、未同意、已撤回或未知。
- 时区：默认 `Asia/Shanghai`，用于外呼时段判断。
- 禁打：开启后系统不得向该客户发起外呼。

已有任务或通话历史的客户不应直接物理删除；需要停止触达时，将其设置为禁打并记录原因。

![新增客户表单](assets/manual-20260908/contacts-form.png)

![填写示例](assets/manual-20260908/contacts-filled.png)

![保存后刷新回显](assets/manual-20260908/contacts-saved.png)

### CSV 导入、导出和批量禁打

入口：管理中心 → 客户运营。

1. 点击“导入”，选择 CSV 文件。
2. 开启“更新已有数据”时，相同号码会更新；关闭时会跳过已有数据。
3. 导入后核对总数、新增、更新、跳过、失败和错误明细。
4. 需要批量禁打时勾选客户，点击“批量禁打”，填写原因后提交。
5. 点击“导出”可下载当前租户客户数据。

正式导入前建议先用 10—20 行样本验证字段、编码、号码格式和去重逻辑，再导入全量数据。

![客户运营导入入口](assets/manual-20260908/contacts-operations.png)

## 7. 话术与流程画布

入口：管理中心 → 话术管理。

![话术管理](assets/manual-20260908/scripts.png)

### 新建基础话术

1. 点击“新建话术”。
2. 填写名称、分类和话术正文。
3. 可补充描述、标签并设置是否启用。
4. 保存后，启用的话术可被外呼任务选择。

![新建话术表单](assets/manual-20260908/scripts-form.png)

![填写示例](assets/manual-20260908/scripts-filled.png)

![保存后刷新回显](assets/manual-20260908/scripts-saved.png)

### 配置话术画布

在话术卡片中点击“话术画布”：

1. 创建画布，或复制已有版本为新草稿。
2. 添加播报、等待、信息采集、设置变量、条件分支、转人工和挂机节点；采集节点需设置变量名称。
3. 选择节点后填写节点名称和播报内容。
4. 设置起点、终点和连线条件：默认、关键词或静默。
5. 点击“保存”保存草稿。
6. 完整模拟并人工复核后再发布；发布版本不可直接修改，需要复制为新草稿。

任务只能绑定已发布的流程版本。发布前应至少覆盖正常回答、拒绝、静默、异常和转人工分支。

![创建画布入口](assets/manual-20260908/script-flow.png)

![创建后的节点和连线](assets/manual-20260908/script-flow-created.png)

![发布前确认](assets/manual-20260908/script-flow-publish-confirm.png)

![已发布版本](assets/manual-20260908/script-flow-published.png)

## 8. 知识库

入口：管理中心 → 知识库。

1. 点击“创建”。
2. 填写标题、分类、标签和知识正文。
3. 开启“启用”并保存。
4. AI 会根据通话内容检索当前租户启用的知识条目。
5. 删除操作会停用知识条目，历史版本仍可追溯。

知识内容应短而明确，避免同一问题存在互相冲突的答案。政策、价格和合规口径变更后应同步更新版本。

![知识库入口](assets/manual-20260908/knowledge.png)

![新增知识表单](assets/manual-20260908/knowledge-form.png)

![填写示例](assets/manual-20260908/knowledge-filled.png)

![保存后刷新回显](assets/manual-20260908/knowledge-saved.png)

## 9. 外呼线路

入口：管理中心 → 外呼线路。

![外呼线路](assets/manual-20260908/lines.png)

点击“新增线路”，配置：

| 字段 | 说明 |
|---|---|
| 名称 | 便于运营识别的线路名称 |
| 服务商 | `HTTP Bridge` 为真实网关接口；`Mock` 仅用于测试 |
| 网关地址 | 语音网关或运营商接口地址 |
| 凭据引用 | 环境变量或密钥管理服务中的引用名，不填写明文密钥 |
| 主叫号码 | 经运营商报备并允许外呼的号码 |
| 并发数 | 该线路最多同时占用的通话数 |
| 优先级 | 数字越小越优先参与选线 |
| 权重 | 同优先级线路间的分配权重 |
| 启用 | 只有启用线路参与真实调度 |

实际准入还受平台硬上限、健康媒体容量、节点和网关预算约束；500 总在途包含呼出、振铃和接通。线路保存成功不代表真实 SIP/RTP 已接通，上线前必须实拨验证。

![新增线路表单](assets/manual-20260908/lines-form.png)

## 10. 系统配置

入口：管理中心 → 系统配置。

![系统配置与并发容量](assets/manual-20260908/settings-tab-0.png)

### 并发容量

- “租户最大同时通话数”允许填写 `1—10000`，保存后立即生效。
- 页面同时显示实际生效并发、线路容量、当前活跃通话、可用槽位和限制来源。
- `10000` 是配置校验上限，不代表系统已经通过万路压测。
- 增加容量前必须同步确认 PBX、运营商、ASR、TTS、数据库、Redis、服务器和网络带宽。

### AI 与语音

配置 AI 服务商、模型、ASR/TTS 服务商、语言和语音相关参数。密钥不得填写在页面中，应通过环境变量或密钥管理服务注入。

![AI与语音配置](assets/manual-20260908/settings-tab-1.png)

### 短信配置

设置短信服务商、签名、模板和挂断短信策略。正式启用前验证模板报备、发送回执、失败重试和计费口径。

![短信配置](assets/manual-20260908/settings-tab-2.png)

### 合规策略

重点检查：

- 允许外呼的开始和结束小时
- 租户时区
- 是否强制 DNC
- 是否要求明确同意
- 单号码每日最大尝试次数
- 同号码两次尝试的最小间隔秒数
- 是否播放录音告知
- 录音保留天数
- 临时转写保留小时数

修改合规策略后，使用测试号码验证允许时段、禁止时段、DNC、未同意、次数和间隔拦截，不要只检查保存成功。

![合规配置](assets/manual-20260908/settings-tab-3.png)

### 接口与回调

设置业务 Webhook 地址、凭据引用、超时、重试次数和退避时间。凭据字段保存引用名，不保存明文密钥。生产启用前需要验证签名、幂等、超时重试、失败队列和接收方对账。

![回调配置](assets/manual-20260908/settings-tab-4.png)

## 11. 创建和运行外呼任务

入口：管理中心 → 外呼任务。

![外呼任务](assets/manual-20260908/campaigns.png)

### 创建任务

1. 点击“创建任务”。
2. 填写任务名称。
3. 选择启用的话术模板和已发布的流程版本，或填写基础话术。
4. 选择模式：录音转人工、纯人工、纯 AI、AI 转人工或 AI + 短信。
5. 选择已同意且非禁打的客户。
6. 设置任务并发、重试次数、重试间隔和同号码尝试间隔。
7. 按业务和法规决定是否录音、是否发送挂断短信。
8. 保存后任务进入草稿状态。

![创建任务完整表单](assets/manual-20260908/campaigns-form.png)

![填写示例](assets/manual-20260908/campaigns-filled.png)

![保存后刷新回显](assets/manual-20260908/campaigns-saved.png)

### 启动前检查

- 客户同意状态和 DNC 正确。
- 当前时间同时满足系统合规外呼时段和已发布业务策略的服务时段；业务策略时段外会继续排队，刷新不能绕过限制。
- 话术/流程版本已经发布并完成模拟。
- 真实线路已启用，主叫号码和凭据正确。
- 租户、任务和线路并发已按压测结果设置。
- 录音告知、短信模板和业务回调已验证。
- 座席状态为“空闲”，转人工链路可用。

### 启动、暂停、恢复和停止

- 点击“启动”会创建并投递通话任务，属于真实业务动作。
- 运行中可暂停；暂停后不会继续投递新的通话。
- 点击恢复可继续处理未完成任务。
- 点击停止结束任务，不再继续调度。
- 运行中的任务不能直接编辑或删除。

![启动确认](assets/manual-20260908/campaign-start-confirm.png)

![运行中](assets/manual-20260908/campaign-running.png)

![暂停后可恢复](assets/manual-20260908/campaign-paused.png)

![恢复运行](assets/manual-20260908/campaign-resumed.png)

![停止后状态](assets/manual-20260908/campaign-stopped.png)

## 12. 单路呼叫和通话记录

管理员入口：管理中心 → 通话记录。
座席入口：座席中心 → 座席工作台或通话记录。

![管理员通话记录](assets/manual-20260908/calls.png)

### 发起单路呼叫

1. 点击“发起外呼”或在座席工作台填写手机号。
2. 选择通话模式、可选任务和客户。
3. 设置最大尝试次数。
4. 确认号码、客户授权、时段和线路后提交。

![发起单路外呼表单](assets/manual-20260908/calls-form.png)

### 通话状态和操作

- 通话记录显示号码、模式、状态、尝试次数、任务和创建时间。
- 点击“事件”查看状态时间线、AI 分析、质检结果和内部事件。
- 仅在状态允许时才能转人工或挂断，禁用按钮表示当前状态不允许该动作。
- 失败、无应答等状态可按策略重拨；系统仍会重新执行合规与频控检查。
- 管理员可点击“导出通话证据”，下载近 30 天 CSV。导出行为会记录到审计日志。

不要把 `queued`、`dialing` 或 `answered` 当作业务完成；以运营商终态、录音、转写和业务回传共同确认结果。

## 13. 座席工作台与人工接管

![座席工作台](assets/manual-20260908/agent-workspace.png)

### 座席状态

- 空闲：可以接受人工接管。
- 忙碌：正在处理会话，不应接收新接管。
- 离线：退出接管队列。

平台会周期性同步座席状态；同步失败时页面会回滚到上一个状态并提示重试。

### 接管流程

1. 座席登录后将状态设为“空闲”。
2. 在“待接管队列”查看客户、号码、任务、意图、最后一句话和摘要。
3. 确认能够接听后点击“接受”；接受后座席状态转为忙碌。
4. 无法处理时点击“拒绝”，请求会按系统规则继续流转。
5. 在“当前会话”持续查看会话状态；处理结束后恢复为空闲。

当前仓库已实现浏览器 WebRTC 软电话、耳麦选择、接管请求和媒体状态控制；真实 WSS、coturn、FreeSWITCH 音频、耳麦、网络抖动和媒体桥接仍需在目标设备上验收。

以下接管截图使用受控Mock回调；它们演示操作顺序，不证明真实PBX接通。

![请求转人工](assets/manual-20260908/call-handoff-request.png)

![座席等待接管队列](assets/manual-20260908/agent-handoff-pending.png)

![座席接受操作](assets/manual-20260908/agent-handoff-accepted.png)

![挂断后话单](assets/manual-20260908/call-ended.png)

![通话事件记录](assets/manual-20260908/call-events.png)

## 14. 质检复核

入口：管理中心 → 质检复核；具备班组长权限的座席也可进入质检复核。

1. 按待复核、已复核或全部筛选。
2. 可设置最高分阈值，优先查看低分通话。
3. 点击“证据”查看结果、意图、情绪、分数、风险标记、摘要、转写和录音资产。
4. 点击“复核”修正通话结果、意图、情绪、质检分、风险标记和摘要。
5. 保存后复核结果会进入仪表盘和运营统计。

人工复核必须基于录音和转写证据。没有可用录音或转写时，应记录证据缺失，不要仅凭自动摘要定责。

![质检复核队列](assets/manual-20260908/quality.png)

## 15. 运营报表、分组监控和账单

### 运营报表

可按任务、座席或线路查看 7、30、90 天数据，并切换按天或按小时粒度。核心指标包括通话、有效接通、转人工、完成、失败、无应答和损失。

![运营报表](assets/manual-20260908/reports.png)

### 分组监控

按客户标签/分组查看客户数、禁打客户、通话、接通、转人工、完成、失败和无应答。发现异常时先检查分组定义、时间范围和样本量。

![分组监控](assets/manual-20260908/group-monitor.png)

### 账单与计费

按任务、座席或线路输入线路分钟单价、AI 分钟单价和短信单价，查看估算成本。该页面属于运营估算，不替代供应商正式账单；上线后应定期与运营商、ASR/TTS、模型和短信账单对账。

![账单与计费](assets/manual-20260908/billing.png)

## 16. 监控与审计

入口：管理中心 → 监控与审计。

![监控与审计](assets/manual-20260908/system.png)

重点关注：

- 数据库、Redis、AI 服务和外呼线路健康状态
- 启用用户与启用线路数量
- 通话状态分布
- 已配置容量、实际生效并发、活跃通话和可用槽位
- AI 平均响应时延、超时任务、最早未完成任务和录音删除失败
- 管理员操作审计日志
- 短信发送状态、供应商消息 ID、错误和失败重试

当页面显示 `mock` 或 `unconfigured` 时，表示对应真实服务没有完成生产配置。遇到故障先暂停新任务，再保存日志、请求 ID、通话 ID和供应商 ID，避免反复点击造成重复操作。

## 17. 常见问题处理

| 现象 | 建议检查 |
|---|---|
| 无法登录 | 入口角色、账号启用状态、密码、登录限流、服务健康 |
| 任务无法启动 | 客户、话术、任务状态、外呼时段、DNC、同意状态、容量和线路 |
| 实际并发低于配置 | 任务并发、租户容量、启用线路并发总和和正在占用的通话 |
| 设置 10000 仍未达到万路 | 10000 只是配置上限；核查 PBX、运营商、ASR/TTS、服务器、网络和压测结果 |
| 转人工按钮不可用 | 通话状态、座席是否空闲、接管请求状态和角色权限 |
| 没有录音或转写 | 录音开关、告知与授权、供应商回调、对象存储和保留策略 |
| 短信失败 | 服务商配置、模板报备、余额、号码格式、回执和重试日志 |
| 统计与业务不一致 | 时间范围、指标口径、运营商终态、自动分析和人工复核结果 |

## 18. 正式上线前置条件

- 使用正式域名、HTTPS、可信主机和最小权限账号。
- 设置 `ENV=production`、关闭演示账号，并替换所有默认密码和密钥。
- 接入真实 PBX/SIP/RTP、运营商号码、ASR、TTS、模型、短信和对象存储。
- 完成号码资质、录音告知、客户授权、DNC、呼叫时段和目标地区法规审查。
- 完成纯人工、录音转人工、纯 AI、AI 转人工和 AI + 短信关键链路实测。
- 按候选容量的 10%、25%、50%、100% 逐级压测，并完成高峰长稳测试。
- 验证故障降级、超时回收、重试幂等、重复回调、备份恢复和审计留痕。
- 在目标浏览器、耳麦、网络和真机上验证 WebRTC 与人工接管。
- 建立运营商、云服务和业务系统的监控、告警、值班与回滚机制。

## 19. 业务交付策略：从客户回答到人工跟进

入口：管理中心 → 业务交付策略（`/admin/product`）。先配置策略和试跑，再让任务使用它。

### 19.1 设置作用范围与异常处理

1. 顶部“活动编号”留空，编辑租户默认策略；填写活动编号则编辑该活动策略。
2. 在“对话与异常处理”填写策略名称、无应答等待、最多澄清次数、置信度门槛、模型等待提示和服务失败结束语。
3. “全局问答”填写 JSON，例如 `{"你们是谁":"我们是客户服务团队。"}`；回答后返回未完成的采集问题。
4. 根据实际供应商设置语言、音色、热词编号。页面不代替供应商账户配置。

![对话与异常处理](assets/manual-20260908/product-tab-0.png)

### 19.2 配置信息采集

1. 点击“信息采集”→“添加字段”。
2. 填写字段标识（如 `city`）、名称、类型和询问话术。
3. 选择是否必填、复述确认；选项类型填写可选值，业务需要筛选时设置“合格取值”。
4. 信息已采集并不自动等于合格线索；合格取值为空时尤其不能这样判断。

![信息采集字段填写示例](assets/manual-20260908/product-slots-filled.png)

### 19.3 设置转人工、服务时间与重拨

进入“转人工与时间安排”，填写可接待坐席编号、最长等待、超时返回AI或结束通话，以及服务时区、工作日、不服务日期和各失败原因的重拨间隔。

![转人工与时间安排](assets/manual-20260908/product-tab-2.png)

`waiting_human` 表示等待接管，必须由实际坐席/PBX接通确认才能算人工已接通。

### 19.4 发布、刷新、试跑与版本回退

1. 点击“发布策略”；成功后刷新页面，确认名称、字段和版本回显。
2. 打开“试跑与版本”，每行输入一轮客户回答，点击“运行对话试跑”。
3. 核对客户说法、意图、动作和回复；至少包含拒绝联系、拒绝转人工、正常采集和异常回答。
4. 试跑使用业务规则，不拨号、不调用模型、不发短信；模型理解步骤和真实音频需单独验证。
5. 回退时点击旧版本“载入此版”，检查后重新发布，生成新版本。遇到版本冲突先刷新并合并，不能覆盖另一位管理员的修改。

![试跑与历史版本](assets/manual-20260908/product-tab-3.png)

![实际发布后回显](assets/manual-20260908/product-policy-saved.png)

![输入拒绝转人工和停止联系后的试跑结果](assets/manual-20260908/product-simulation-result.png)

### 19.5 预约回拨

在“预约回拨”查看号码、预约时间和状态。已确认预约可修改时间并点击“保存时间”；取消需二次确认。刷新后检查状态。页面管理已有预约，不能把系统生成的预约记录当成真实拨号成功。

![合成预约记录](assets/manual-20260908/appointment-before.png)

![填写预约时间](assets/manual-20260908/appointment-time-filled.png)

![保存后回显](assets/manual-20260908/appointment-saved.png)

![取消后回显](assets/manual-20260908/appointment-cancelled.png)

### 19.6 人工跟进

打开“人工跟进”，核对来源、号码、内容和状态。业务人员完成实际回访后点击“标记完成”，刷新确认 `completed`。点击按钮只记录跟进状态，不会自动拨电话。

![待处理跟进（API合成事件生成）](assets/manual-20260908/followup-pending.png)

![实际点击完成后刷新回显](assets/manual-20260908/followup-completed.png)

### 19.7 交付统计与交付结果

“交付统计”查看接通尝试和去重号码；组内唯一号码不能跨组直接相加作为全局去重数。“交付结果”核对拨打轮次、策略版本、接听类型、业务结果和已确认采集信息。未知/待确认需要人工核实。

![交付统计](assets/manual-20260908/product-tab-6.png)

![交付结果](assets/manual-20260908/product-tab-7.png)

### 19.8 知识导入与检索测试

1. 打开“知识导入与检索测试”，填写标题、出处、正文，或选择 `.txt` / `.md` 文件；单文件不超过150000字节。
2. 点击“保存知识”。
3. 输入检索问题并点击“测试检索”，核对匹配标题和正文。
4. 保存与检索通过后，再在真实会话中检查答案是否遵循审核后的知识口径。

![填写知识](assets/manual-20260908/product-knowledge-filled.png)

![保存后的检索结果](assets/manual-20260908/product-knowledge-result.png)

## 20. 本次操作验收与边界

入口清单：管理中心16个菜单、座席工作台/通话记录、两类登录、系统配置5个标签、业务交付策略9个标签，以及客户、用户、线路、知识、话术、任务、单路呼叫表单。

本项目没有门店、房型、购物车、支付和订单页面；通用电商验收链不适用。使用“登录→客户搜索/创建→话术/知识→任务→通话→接管→质检/跟进→报表→退出”作为本项目业务链。

本次详细已验证/未验证、问题、环境限制和上线前置条件见[验收记录](reviews/20260908-manual-refresh.md)。截图只证明当时可见页面；真实电话、云ASR/TTS/LLM、短信到达、耳麦、生产与500总在途容量不能据此判定通过。
