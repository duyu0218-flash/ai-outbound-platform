# 业务能力修复与本地验收

日期：2026-09-08。基础提交：`547abb4`。目标分支：`fix/review-p1-p2-20260907`。

本次完成已复现的意向误判、否定转接、业务动作缺失，以及业务策略、状态持久化和交付通知的核心改造。范围来自 [产品能力差距报告](20260908-product-capability-gap.md)。该报告还包含完整产品扩展建议；本记录逐项说明实现程度，不能将本次提交等同为原报告全部扩展能力已经商用。

## 修复及实现范围

| 项目 | 本次实现 | 验证与边界 |
|---|---|---|
| P1-01 意图及业务动作 | 否定、更正、提问、允许继续听与业务兴趣分开；停止触达、打错号码形成租户号码禁呼；取消未拨任务、取消预约；回拨时间需确认；告别/拒绝转人工有独立动作 | 已测原报告复现样例、低置信度、并发禁呼。采用保守规则，未知表达交给模型或澄清，不承诺通用语义识别准确率 |
| P1-02 业务字段 | 类型化字段、必填、选项、确认、合格值；显式多字段回答逐项确认；保留原始话轮/时间/拨打轮次证据 | 已测采集与确认、否认和多字段。任意自然语言自动抽取仍有边界；不凭“可以听一下”生成合格线索 |
| P1-03 静默及拒识 | 播放后启动持久化提醒；客户开口/新转写取消旧计时；“稍等”有限延长；空最终转写与低置信度先澄清；达到次数结束 | 已测旧任务失效、重试不重复累计、空转写。真实噪声、咳嗽、长停顿需语音样本验收 |
| P1-04 非真人 | 电话助手/信箱/分机提示进入确认分支；无法确认保持 unknown，重复提示或配置后结束；不计为合格线索；永久失败原因停止重拨 | 已测文本提示场景；未交付声学蜂鸣/真人识别模型、自动分机后续导航和可配置留言流程 |
| P1-05 转人工 | 按业务策略配置坐席名单及服务时间；等待提示、最长等待、回 AI 或结束；等待中取消；摘要携带已确认字段；释放坐席时核对其他通话归属 | 已测等待取消/归属保护，浏览器 mock 链路另见结果。名单是按业务配置的路由基础，尚无独立多维技能组管理；转接已发往 PBX 后的撤销及真实桥接需进一步实现/验收 |
| P1-06 服务故障 | 模型等待一次提示；模型失败结束语；TTS 失败走 FreeSWITCH 已安装 WAV 兜底，再尝试挂断；ASR 错误进入有界结束；人工跟进记录；没有 PBX 结束证据不释放容量 | 已测模型等待/过期取消、故障容量保护、WAV 命令绕过 TTS。真实播放须部署自有提示音并验证声音 |
| P2 语音策略 | 按活动/租户配置允许打断、最少字数、开场延迟、断句、NLS 热词表、服务语言与音色 | 已接入媒体参数；未交付逐节点独立语速、打断时长和数字读法编辑器；语言/音色仍受所选云服务实际支持范围约束 |
| P2 流程能力 | collect/set/branch 节点、变量与等值/不等值/意图条件、变量替换；自动循环发布前拒绝；同轮重试复用决策；全局 FAQ 后回到采集问题 | 已测服务函数，画布页面验收见下表；没有将不确定模型文本直接写成已确认字段 |
| P2 知识运营 | 文本/Markdown 导入；出处、活动范围、生效时间；检索试跑；发布时冻结知识快照 | 已测范围、过期排除、版本冻结；检索为词面/中文片段召回，未交付向量语义检索、PDF/OCR/Word 文档解析 |
| P2 排程重拨 | 时区、工作日、节假日、服务时段；回拨预约确认、改期、取消及修订号；到期执行与稳定拨打标识；过期/禁呼不拨；按原因配置重拨间隔 | 已测精确/模糊时间、确认、取消修订、页面改期；真实定时拨号仍需队列工作进程和线路联合验收 |
| P2 发布追溯 | 不可变策略版本、并发发布冲突、基础语义样例门禁、完整文本规则试跑、载入旧版再发布；在途会话冻结 AI 配置、话术和知识 | 已测版本冻结与过期发布拒绝；尚无可保存的自定义回归套件和真实整段语音试跑工作台、专门的版本差异页 |
| P2 交付结果 | 当前拨打轮次分析；有效线索和仅有兴趣区分；录音待到/待复核/ready；人工复核与后到证据产生内容版本；结果与通知 Outbox 同事务；尝试次数与组内唯一号码分开统计 | 已测重复分析通知去重、人工修改新版本、旧轮次隔离、PostgreSQL 聚合；当前没有完整的节点流失漏斗和接收方交付回执工作台 |
| 可选扩展 | DTMF 数字字段；短信回复/来电事件幂等入口；关联最近通话并创建人工跟进；短信退订即使没有历史通话也进入禁呼 | 已测 DTMF、来信冲突、角色权限、工单回显。`inbound_call` 是业务事件接入，尚未实现完整 SIP 呼入机器人接听；声音克隆和多智能体接力未实现 |

## 页面、入口与状态清单

| 页面/入口 | 覆盖的操作与状态 |
|---|---|
| `/admin/product` 对话与异常处理 | 导航点击、完整内容、策略名称/FAQ/等待参数、发布成功、刷新回显、过期版本 409 |
| 信息采集 | 添加字段、标识/名称/问题填写、确认设置、发布与回显 |
| 转人工与时间安排 | 配置读取、服务时区/时间/坐席名单/等待与失败策略；参数规则通过后端校验。未逐个控件做浏览器组合穷举 |
| 试跑与版本 | 多轮文本试跑、回复/动作/意图展示；历史版本读取和载入后重新发布机制 |
| 预约回拨 | 预约列表、填写日期、保存改期、刷新、取消确认、取消后按钮禁用、修订冲突 |
| 人工跟进 | 来信入库、重复事件去重、同事件不同内容 409、列表回显、完成保存和刷新 |
| 知识导入与检索 | 标题/正文/出处填写与保存、检索显示匹配条目；文件读取限大小 |
| 交付结果/统计 | 按轮次和业务结果查询、服务端分页、组内唯一号码；统计接口已在 SQLite/PostgreSQL 验证 |
| `/admin/scripts` 话术画布 | 新节点入口、变量配置、保存和重开回显；服务端图结构和条件执行测试 |
| 既有关键业务 | 登录 → 客户创建/查询 → 活动编辑/启停 → 话单 → mock 转人工 → 记录；退出、返回、错误登录、跨租户切换 |

原验收模板中的门店、购物车、订单、支付属于电商链路，本仓库无这些功能；本次采用上表的外呼业务链路，没有虚报电商验收。

## 已验证

- 后端完整回归：230 passed、7 skipped。跳过包含 2 个显式 PostgreSQL 测试（另行通过），其余依赖可选测试环境；未把 skipped 算通过。
- PostgreSQL 专项：2 passed。8 个不同通话同时对同一号码禁呼，最终仅一条禁呼记录，未错误结束其他在途通话；迁移 SQL 连续执行两遍、统计聚合通过。
- Agent：8 passed；固定无 NLTK Pipecat 环境的网关：211 passed。
- 前端 TypeScript 与 Vite 构建通过；Vitest：11 passed。
- Chromium 完整入口及表单测试：19 passed，无跳过，包含 mock 转人工、活动启停、跨租户切换及新增采集画布。
- `git diff --check` 通过。

证据目录：[测试输出和源码指纹](evidence/20260908-product)。这些是隔离数据库、mock 电话/模型或合成输入的测试，不能替代真实声音和容量验收。

## 环境及发布状态

| 状态 | 本次结果 |
|---|---|
| 源代码 | 已修改，新增数据库迁移与回归测试 |
| 静态检查 | 已通过 |
| 开发环境 | 隔离 PostgreSQL/Redis + 本机 API/Chromium 验证 |
| 测试环境发布 | 未发布到共享测试环境 |
| 真机 | 未验证 |
| 生产环境 | 未发布 |
| Git | 本地提交；未 push |
| 单机 200 / 全系统 500 路 | 本轮未验证，不作容量保证 |

环境排查记录：首次复用 SQLite 测试文件导致重复数据冲突，改用独立新文件；旧语音虚拟环境包含未批准 Pipecat/NLTK，改为项目固定的 `.venv-no-nltk`；本机端口相关测试在允许 loopback 的隔离环境执行。浏览器原本默认时段为 9–20 点，8 点启动拨号被正常拦截；只将 mock 测试配置改为全天，未修改生产策略。

## 上线前置条件与已知限制

1. 备份并执行 `backend/migrations/postgresql/20260908_product_delivery.sql`，按当前代码版本升级 API、队列工作进程、Agent、媒体进程和网关；不能只替换静态页面。
2. `after_playback` 队列承担提醒和预约；必须运行对应任务工作进程，监控积压、死任务和预约延误。到期因容量不足延迟超过 15 分钟会转人工核实，不悄悄改约定时间。
3. 将自有 WAV 提示音安装到 FreeSWITCH 可读的固定路径，通过 `FREESWITCH_FALLBACK_AUDIO_PATH` 配置网关。仅有配置不等于已经听到提示音。
4. 确认 ASR/TTS/LLM 凭据、音色和语言、NLS 热词表，以及签名回调和结果接收端幂等策略；接收端按通话标识更新结果版本，不按每次回调新增线索。
5. 使用真实 SIP/RTP 和双向声音验收打断、噪声、模型超时、ASR 断开、TTS 故障、转人工掉线/无人接、录音后到及重启恢复。运营商已有 500 路不是本次测试的限制项。
6. 在目标服务器上补实际 200 路同时接通和 500 路集群长稳测试，并分开测 API RPS、CPS、AI session 与通话路数。本轮没有执行新的容量压测。
7. 上表标为“未交付/未实现”的新增产品能力仍未完成，尤其完整 SIP 呼入机器人、技能组管理、声学非真人检测、逐节点语音参数、语音试跑、自定义回归套件、向量检索、声音克隆及多智能体接力。不能以这次本地提交宣称已经与两家云产品全面对齐。

原报告的增强建议仍有未实现项，本次为核心业务修复提交，并非“上述全部功能完成”。

本次保留原有 `backend/ai_outbound.db`，不纳入版本控制。临时隔离测试服务、数据库和 Redis 在验收后清理，既有实例不替换。
